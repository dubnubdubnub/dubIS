# Reference copy of /etc/tmpfiles.d/dubis.conf. The socket's access control
# is this directory: uvicorn chmods the socket itself 0666, so only members
# of the group (uwrl-dev) can traverse /run/dubis to reach it.
d /run/dubis 0750 isaac uwrl-dev -

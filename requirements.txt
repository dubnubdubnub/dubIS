pywebview>=5.0
# Client only -- `playwright install` is deliberately NOT run. The CDP
# backend in browser_page.py attaches to a browser running elsewhere, so
# the ~400MB of bundled browsers would be downloaded and never used.
playwright>=1.44
browser-cookie3>=0.19
xlrd>=2.0
pdfplumber>=0.10
pytesseract>=0.3
Pillow>=10
PyMuPDF>=1.24
opencv-python-headless>=4.9
numpy>=1.26
requests>=2.31
fastapi>=0.110
# Runtime, not just tests, since the multi-server hub: server/sources.py opens an
# httpx.AsyncClient per configured source and server/proxy.py + server/fanout.py
# forward through it. Imported by server/app.py's create_app, so a missing httpx
# is an immediate boot failure, not a degraded feature.
httpx>=0.27
uvicorn[standard]>=0.29
pydantic>=2.6
